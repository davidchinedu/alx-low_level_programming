#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the progrsm
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: always 0 (Success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
print("%s\n", *argv);

return (0);
}
